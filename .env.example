# Example environment variables for Watsonx.ai (copy to `.env` and fill values)
# Replace the placeholder values with your deployment values.

# Watsonx endpoint URL (e.g., https://api.us-south.watsonx.ai)
WATSONX_URL=https://your-watsonx-url

# Watsonx API key (keep secret) - store in `.env`, never commit real keys
WATSONX_APIKEY=your_api_key_here

# Default model id to use (example: 'mistral-large' or another deployed model)
WATSONX_MODEL_ID=openai/gpt-oss-120b

# Project id in Watsonx for context (optional)
WATSONX_PROJECT_ID=your_project_id_here

